<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- CABECERA PERSONALIZADA (simulando Toolbar) -->
    <LinearLayout
        android:id="@+id/custom_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@drawable/header_gradient"
        android:orientation="horizontal"
        android:paddingHorizontal="16dp"
        android:gravity="center_vertical"
        android:layout_alignParentTop="true">

        <!-- Título a la izquierda -->
        <TextView
            android:id="@+id/toolbar_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/app_name"
            android:textColor="@color/white"

            android:textSize="20sp"
            android:textStyle="bold" />

        <!-- Botón de idiomas a la derecha -->
        <LinearLayout
            android:id="@+id/toolbar_lang_container"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginEnd="8dp"
            android:background="@drawable/btn_circle_background_small"
            android:gravity="center"
            android:elevation="4dp">

            <TextView
                android:id="@+id/toolbar_lang_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="🌐"
                android:gravity="center"
                android:textSize="20sp" />
        </LinearLayout>

        <!-- Botón de filtros a la derecha -->
        <LinearLayout
            android:id="@+id/toolbar_filters_container"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/btn_circle_background_small"
            android:gravity="center"
            android:elevation="4dp">

            <ImageButton
                android:id="@+id/toolbar_filters_button"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="@android:color/transparent"
                android:src="@drawable/ic_filter_pro"
                android:contentDescription="@string/content_desc_filters"
                android:scaleType="centerInside"
                app:tint="@color/white" />

        </LinearLayout>

    </LinearLayout>

    <!-- Mapa (debajo de la cabecera) -->
    <org.osmdroid.views.MapView
        android:id="@+id/map"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/custom_toolbar" />

    <!-- PANEL DE DETALLES (Fragment) -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/detail_panel_container"
        android:name="com.eliasbuenosdias.geogas.ui.fragments.DetailsPanelFragment"
        android:layout_width="340dp"
        android:layout_height="match_parent"
        android:layout_alignParentEnd="true"
        android:elevation="12dp"
        android:visibility="gone" />

    <!-- PANEL DE FILTROS (Fragment) -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/filters_panel_container"
        android:name="com.eliasbuenosdias.geogas.ui.fragments.FiltersFragment"
        android:layout_width="360dp"
        android:layout_height="match_parent"
        android:layout_alignParentStart="true"
        android:elevation="12dp"
        android:visibility="gone" />


    <!-- BOTÓN DE UBICACIÓN PROFESIONAL -->
    <LinearLayout
        android:id="@+id/btn_location_container"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_marginBottom="24dp"
        android:layout_marginEnd="24dp"
        android:background="@drawable/btn_circle_background_small"
        android:elevation="8dp"
        android:gravity="center"
        android:orientation="vertical"
        android:visibility="gone">

        <ImageButton
            android:id="@+id/btn_my_location"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="@android:color/transparent"
            android:src="@drawable/ic_location_pro"
            android:contentDescription="@string/content_desc_location"
            android:scaleType="centerInside" />

    </LinearLayout>

    <!-- SPLASH SCREEN -->
    <include
        android:id="@+id/splashScreen"
        layout="@layout/splash_screen" />

</RelativeLayout>